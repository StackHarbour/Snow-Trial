import { SiteNav } from '../components/site-nav';
import { SiteFooter } from '../components/site-footer';

export default function Methodology(){
  return <main className="page-shell"><SiteNav/><section className="method-page"><div className="eyebrow">METHODOLOGY</div><h1>Trust comes from<br/><em>showing the work.</em></h1><p className="lead">Snow Trail is designed around traceable forecast data rather than a single opaque API response. The prototype uses synthetic data, but the production architecture is built to preserve source, freshness, coverage and confidence.</p><div className="method-grid"><article><span>01 / SOURCES</span><h2>Multi-source by design</h2><p>Forecast, geocoding, alerts and map layers are separated behind provider contracts. A production provider can be replaced without rewriting the forecast UI.</p></article><article><span>02 / FRESHNESS</span><h2>Update time is not valid time</h2><p>We distinguish when a provider updated a model from when Snow Trail retrieved it and when the forecast applies.</p></article><article><span>03 / COVERAGE</span><h2>Nearest grid, when exact data isn't available</h2><p>A ZIP or coordinate can resolve to a supported forecast grid. The interface should disclose that geographic resolution instead of claiming impossible precision.</p></article><article><span>04 / CONFIDENCE</span><h2>Uncertainty is part of the answer</h2><p>Confidence can consider source agreement, forecast horizon, freshness and spatial resolution. We will not invent a meaningless accuracy percentage.</p></article><article><span>05 / FAILURE</span><h2>Partial data stays partial</h2><p>If snow data fails while temperature remains available, the temperature remains usable and the snow panel explains the failure.</p></article><article><span>06 / AI</span><h2>AI explains; it doesn't forecast</h2><p>The assistant will receive normalized, source-aware forecast context. It cannot independently invent current conditions or replace the underlying provider.</p></article></div></section><SiteFooter/></main>
}

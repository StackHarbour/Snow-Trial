import Link from 'next/link';
import { ArrowLeft, Database, Gauge, MapPin, RefreshCw, ShieldCheck } from 'lucide-react';

export default function MethodologyPage() {
  return <main><div className="location-topbar"><Link href="/" className="back-link"><ArrowLeft size={16} /> Snow Trail</Link><span>Methodology</span></div><div className="page-width methodology"><header className="method-hero"><span className="eyebrow"><ShieldCheck size={15} /> Data methodology</span><h1>Forecast depth without pretending uncertainty does not exist.</h1><p>Snow Trail is built around a simple rule: a weather value is useful only when the user can understand what produced it, when it was retrieved, and how much uncertainty remains.</p></header><div className="method-grid"><article><Database /><h2>Providers</h2><p>NOAA / National Weather Service is the authoritative U.S. baseline for Phase 1. A provider adapter isolates upstream formats from the product domain model.</p></article><article><MapPin /><h2>Location resolution</h2><p>User input is geocoded to coordinates, then NWS maps those coordinates to the applicable local forecast grid. The UI does not claim ZIP codes are infinitely precise.</p></article><article><RefreshCw /><h2>Freshness</h2><p>Snow Trail records provider/model update time and its own retrieval time separately. Cached responses expose their age.</p></article><article><Gauge /><h2>Confidence</h2><p>Confidence is qualitative. Forecast horizon, data freshness, missing snow-specific values, and source disagreement can reduce confidence. No arbitrary accuracy percentage is manufactured.</p></article></div><section><h2>Source reconciliation</h2><p>Snow Trail does not silently average conflicting providers into a fictional truth. When supplemental providers are added, disagreement becomes metadata and feeds the confidence evaluation.</p></section><section><h2>Why forecasts change</h2><p>Forecast models update, observations change initial conditions, and local forecast grids are revised. A new forecast is not necessarily a correction of an error; it can reflect new information.</p></section><section><h2>Commercial provider boundaries</h2><p>Open-Meteo provides a geocoding and supplemental-weather integration boundary, but its current documentation distinguishes commercial usage from the public/non-commercial path. The project therefore exposes an API-key configuration rather than treating a public endpoint as a commercial entitlement.</p></section><section><h2>Known limitations</h2><ul><li>NWS snow-specific grid values can be missing for some hours/locations.</li><li>The Phase 1 map is a real basemap plus selected-location forecast context, not a proprietary nationwide snowfall raster.</li><li>AI explanation is optional and only receives normalized Snow Trail forecast context.</li><li>No accuracy guarantee is made.</li></ul></section></div></main>;
}

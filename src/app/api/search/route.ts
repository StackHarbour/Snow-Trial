import {NextResponse} from 'next/server';import {searchLocations} from '@/services/geocoding';export async function GET(req:Request){const q=new URL(req.url).searchParams.get('q')||'';return NextResponse.json({results:searchLocations(q)})}

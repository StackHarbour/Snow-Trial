import {NextResponse} from 'next/server';import {getForecastForLocation} from '@/services/forecast-orchestrator';import {explainForecast} from '@/services/ai-explanation';export async function POST(req:Request){const body=await req.json().catch(()=>({}));const d=await getForecastForLocation(String(body.locationId||''));if(!d)return NextResponse.json({error:'Forecast context unavailable'},{status:404});return NextResponse.json({answer:explainForecast(d.forecast),sourceContext:d.forecast.source})}

# No production provider credentials are required for the demo implementation.
# Future server-only values belong here; do not prefix secrets with NEXT_PUBLIC_.
# FORECAST_PROVIDER_API_KEY=
# GEOCODING_PROVIDER_API_KEY=

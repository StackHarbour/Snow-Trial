# Snow Trail

Snow-first forecasting product prototype.

This package is intentionally self-contained and demo-only. Forecast values are synthetic and clearly labeled; no live weather provider is connected.

## Run

```bash
npm install
npm run dev
```

Production checks:

```bash
npm run typecheck
npm run lint
npm run build
```

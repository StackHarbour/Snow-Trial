const input=document.querySelector('#search');const go=document.querySelector('#go');const near=document.querySelector('#near');go.addEventListener('click',()=>{const q=input.value.trim();if(!q){input.focus();return}alert(`Prototype search: ${q}\n\nReal forecast lookup will be connected in the data layer.`)});input.addEventListener('keydown',e=>{if(e.key==='Enter')go.click()});near.addEventListener('click',()=>{if(!navigator.geolocation){alert('Location is not supported by this browser.');return}near.textContent='Requesting location…';navigator.geolocation.getCurrentPosition(()=>{near.textContent='◎ Conditions Near Me';alert('Prototype location flow: coordinates received. Forecast-grid lookup will be connected later.')},()=>{near.textContent='◎ Conditions Near Me';alert('Location permission was denied. You can continue by searching a location.')})});
